<Window x:Class="UniclassClassifier.Windows.InconsistenciesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Classification Inconsistencies" 
        MinHeight="400"
        MaxHeight="700"
        SizeToContent="Height"
        Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E0E0E0"/>
            <Setter Property="AlternatingRowBackground" Value="#F9F9F9"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="DataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#34495E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="#2C3E50"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
        </Style>

        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#3498DB"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Classification Inconsistencies Detected" 
                       Style="{StaticResource HeaderTextStyle}"/>
            <TextBlock Text="The following Family Types have instances with different classifications. Review before consolidation." 
                       FontSize="12" 
                       Foreground="#7F8C8D" 
                       TextWrapping="Wrap"
                       Margin="0,0,0,15"/>
        </StackPanel>

        <!-- Warning Box -->
        <Border Grid.Row="1" 
                Background="#FEF5E7" 
                BorderBrush="#F39C12" 
                BorderThickness="2" 
                CornerRadius="5" 
                Padding="15" 
                Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Warning Icon -->
                <Border Grid.Column="0"
                        Width="28" 
                        Height="28"
                        Background="#F39C12"
                        CornerRadius="14"
                        Margin="0,0,12,0"
                        VerticalAlignment="Center">
                    <TextBlock Text="!" 
                               FontSize="18" 
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Margin="0,-2,0,0"/>
                </Border>
                
                <TextBlock Grid.Column="1"
                           x:Name="InconsistentCountText" 
                           Text="0 Family Types with inconsistent classifications" 
                           FontSize="14" 
                           FontWeight="SemiBold" 
                           Foreground="#D68910" 
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- DataGrid with MaxHeight for dynamic sizing -->
        <DataGrid x:Name="InconsistenciesDataGrid"
                  Grid.Row="2"
                  Style="{StaticResource DataGridStyle}"
                  ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                  CellStyle="{StaticResource DataGridCellStyle}"
                  MaxHeight="450">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Category" 
                                    Binding="{Binding Category}" 
                                    Width="95"/>
                <DataGridTextColumn Header="Family and Type" 
                                    Binding="{Binding FamilyTypeKey}" 
                                    Width="230"/>
                <DataGridTextColumn Header="Instances" 
                                    Binding="{Binding InstanceCount}" 
                                    Width="75">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Different" 
                                    Binding="{Binding UniqueClassificationsCount}" 
                                    Width="70">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="#E74C3C"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Most Common" 
                                    Binding="{Binding MostCommonClassification}" 
                                    Width="115">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Foreground" Value="#3498DB"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Highest Confidence" 
                                    Binding="{Binding HighestConfidenceClassification}" 
                                    Width="135">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Foreground" Value="#9B59B6"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Description" 
                                    Binding="{Binding MostCommonDescription}" 
                                    Width="*"
                                    MinWidth="170"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons -->
        <Grid Grid.Row="3" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" 
                       FontSize="11" 
                       Foreground="#7F8C8D" 
                       VerticalAlignment="Center"
                       TextWrapping="Wrap">
                <Run Text="These inconsistencies will be resolved during consolidation. You can choose between "/>
                <Run Text="Most Common" FontWeight="SemiBold" Foreground="#3498DB"/>
                <Run Text=" or "/>
                <Run Text="Highest Confidence" FontWeight="SemiBold" Foreground="#9B59B6"/>
                <Run Text=" for each Family Type in the next step."/>
            </TextBlock>

            <Button x:Name="CancelButton"
                    Grid.Column="1"
                    Content="Cancel"
                    Height="40"
                    Width="120"
                    Margin="10,0,5,0"
                    Background="#95A5A6"
                    Foreground="White"
                    FontSize="13"
                    FontWeight="SemiBold"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="CancelButton_Click"/>

            <Button x:Name="ContinueButton"
                    Grid.Column="2"
                    Content="Continue"
                    Height="40"
                    Width="120"
                    Margin="5,0,0,0"
                    Background="#27AE60"
                    Foreground="White"
                    FontSize="13"
                    FontWeight="SemiBold"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="ContinueButton_Click"/>
        </Grid>
    </Grid>
</Window>
