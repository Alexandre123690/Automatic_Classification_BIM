<Window x:Class="UniclassClassifier.Windows.ConsolidationConfirmWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm Type Classification" 
        Height="750" 
        Width="1300"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <Style x:Key="DataGridStyle" TargetType="DataGrid">
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E0E0E0"/>
            <Setter Property="AlternatingRowBackground" Value="#F9F9F9"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="DataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#34495E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="#2C3E50"/>
            <Setter Property="BorderThickness" Value="0,0,1,0"/>
        </Style>

        <!-- Updated DataGridCellStyle - No blue background on selection -->
        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="{Binding RelativeSource={RelativeSource Self}, Path=Foreground}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Confirm Type-Level Classification" 
                       Style="{StaticResource HeaderTextStyle}"/>
            <TextBlock Text="Review the consolidated classifications below. Each row shows both the Most Common and Highest Confidence classifications. Select the desired code from the dropdown, choose an existing option, or enter a custom code. The description updates automatically." 
                       FontSize="12" 
                       Foreground="#7F8C8D" 
                       TextWrapping="Wrap"
                       Margin="0,0,0,15"/>
        </StackPanel>

        <!-- Stats Info -->
        <Border Grid.Row="1" 
                Background="#EBF5FB" 
                BorderBrush="#3498DB" 
                BorderThickness="2" 
                CornerRadius="5" 
                Padding="15" 
                Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border Width="28" 
                        Height="28"
                        Background="#3498DB"
                        CornerRadius="14"
                        Margin="0,0,12,0"
                        VerticalAlignment="Center">
                    <TextBlock Text="?" 
                               FontSize="18" 
                               FontWeight="Bold"
                               Foreground="White"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Margin="0,-2,0,0"/>
                </Border>
                <TextBlock x:Name="TypeCountText" 
                           Text="0 Family Types ready for consolidation" 
                           FontSize="14" 
                           FontWeight="SemiBold" 
                           Foreground="#3498DB" 
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Quick Set Dropdown -->
        <Border Grid.Row="2" 
                Background="White" 
                BorderBrush="#BDC3C7" 
                BorderThickness="1" 
                CornerRadius="4" 
                Padding="12,10" 
                Margin="0,0,0,15">
            <Grid HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="200"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0"
                           Text="Quick Action:" 
                           FontSize="13" 
                           FontWeight="SemiBold"
                           Foreground="#2C3E50"
                           VerticalAlignment="Center"
                           Margin="0,0,15,0"/>
                
                <TextBlock Grid.Column="1"
                           Text="Set all rows to:" 
                           FontSize="12" 
                           FontWeight="Normal"
                           Foreground="#7F8C8D"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Right"
                           Margin="0,0,10,0"/>
                
                <ComboBox x:Name="SetAllComboBox"
                          Grid.Column="2"
                          Height="32"
                          FontSize="12"
                          VerticalContentAlignment="Center"
                          Padding="10,5"
                          SelectionChanged="SetAllComboBox_SelectionChanged">
                    <ComboBoxItem Content="Highest Confidence" Tag="HighestConfidence" IsSelected="True"/>
                    <ComboBoxItem Content="Most Common" Tag="MostCommon"/>
                </ComboBox>
            </Grid>
        </Border>

        <!-- DataGrid with dynamic colors -->
        <DataGrid x:Name="ConsolidationDataGrid"
                  Grid.Row="3"
                  Style="{StaticResource DataGridStyle}"
                  ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                  CellStyle="{StaticResource DataGridCellStyle}"
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Category" 
                                    Binding="{Binding Category}" 
                                    Width="95"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Family and Type" 
                                    Binding="{Binding FamilyTypeKey}" 
                                    Width="200"
                                    IsReadOnly="True"/>
                <DataGridTextColumn Header="Instances" 
                                    Binding="{Binding InstanceCount}" 
                                    Width="70"
                                    IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                
                <!-- Most Common (Blue - Static Color) -->
                <DataGridTextColumn Header="Most Common" 
                                    Binding="{Binding MostCommonClassification}" 
                                    Width="115"
                                    IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Foreground" Value="#3498DB"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                
                <!-- Highest Confidence (Purple - Static Color) -->
                <DataGridTextColumn Header="Highest Conf." 
                                    Binding="{Binding HighestConfidenceClassification}" 
                                    Width="115"
                                    IsReadOnly="True">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Foreground" Value="#9B59B6"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                
                <!-- Classification Code Dropdown (Blue Background) -->
                <DataGridTemplateColumn Header="Selected Code" Width="150">
                    <DataGridTemplateColumn.HeaderStyle>
                        <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource DataGridColumnHeaderStyle}">
                            <Setter Property="Background" Value="#3498DB"/>
                        </Style>
                    </DataGridTemplateColumn.HeaderStyle>
                    <DataGridTemplateColumn.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Background" Value="#EBF5FB"/>
                            <Setter Property="Padding" Value="5,5"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        </Style>
                    </DataGridTemplateColumn.CellStyle>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding AvailableCodes}"
                                      SelectedItem="{Binding SelectedCode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      Height="28"
                                      FontSize="10"
                                      Padding="6,2"
                                      Background="White"
                                      IsEditable="True"
                                      LostFocus="CodeComboBox_LostFocus"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                
                <!-- Dynamic Description - Always black except custom (purple) -->
                <DataGridTemplateColumn Header="Description (Auto)" 
                                        Width="*"
                                        MinWidth="220">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding CurrentDescription}" 
                                       FontStyle="Italic"
                                       TextWrapping="Wrap"
                                       Padding="5,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <!-- Default: Black -->
                                        <Setter Property="Foreground" Value="#2C3E50"/>
                                        <Style.Triggers>
                                            <!-- Only custom codes get purple color -->
                                            <DataTrigger Binding="{Binding IsSelectedCodeCustom}" Value="True">
                                                <Setter Property="Foreground" Value="#9B59B6"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons -->
        <Grid Grid.Row="4" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" 
                       FontSize="11" 
                       Foreground="#7F8C8D" 
                       VerticalAlignment="Center"
                       TextWrapping="Wrap">
                <Run Text="This will apply classifications to "/>
                <Run Text="Type-level parameters" FontWeight="SemiBold" Foreground="#3498DB"/>
                <Run Text=" and automatically "/>
                <Run Text="synchronize all instance parameters" FontWeight="SemiBold" Foreground="#27AE60"/>
                <Run Text=" with the type values."/>
            </TextBlock>

            <Button x:Name="CancelButton"
                    Grid.Column="1"
                    Content="Cancel"
                    Height="40"
                    Width="120"
                    Margin="10,0,5,0"
                    Background="#95A5A6"
                    Foreground="White"
                    FontSize="13"
                    FontWeight="SemiBold"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="CancelButton_Click"/>

            <Button x:Name="ApplyButton"
                    Grid.Column="2"
                    Content="Apply to Types"
                    Height="40"
                    Width="140"
                    Margin="5,0,0,0"
                    Background="#27AE60"
                    Foreground="White"
                    FontSize="13"
                    FontWeight="SemiBold"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="ApplyButton_Click"/>
        </Grid>
    </Grid>
</Window>
